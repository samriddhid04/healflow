{
  "name": "healflow-backend",
  "version": "1.0.0",
  "description": "HealFlow Healthcare SaaS - Express API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint . --ignore-pattern node_modules --max-warnings 10",
    "lint:fix": "eslint . --fix --ignore-pattern node_modules",
    "test": "echo 'No tests yet — add Jest here' && exit 0",
    "audit:check": "npm audit --audit-level=high"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^3.0.2"
  }
}